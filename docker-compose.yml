version: '3'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    restart: always
  db:
    image: mariadb
    env_file:
    - .env
    volumes:
      - ./data:/var/lib/mysql
    ports:
      - "3001:3306"
