// We don't really need this anymore, but still keeping it
// around for whenever I need to write another middleware.

/*
// It's a module, dummy!
export {}

// Augument Express with custom stuff for middleware
declare global {
	namespace Express {
		interface Request {
			account: Record<string, string>;
		}
	}
}*/
